TODO list for the next update:
    (!)Add Bayonet to RightKnifeAntiCheat(!) ✓ - Needs test
    Add Debug cvar ✓
    Add Debug texts ✓
    (!)Add Support to CSS thirdperson knife(!) ✓
    (!)Add reset to thirdperson used cvar(!) ✓
    (!)Fix the Outside LR slay(!) ?
    Fix the Race AirKnife Slay ✓
    (!)Fix MyJailBreak support(!)
    Translate it to Hungarian - Needs more Translations
    Make Texts fancy
    Create custom prefix for servernames
    (+) Colored the Debug texts ✓
